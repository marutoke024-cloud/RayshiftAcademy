# OS / editor
Thumbs.db
.DS_Store
*.log

# Local secrets (do NOT commit real Firebase keys here if you prefer to keep them private)
# js/config/firebase-config.local.js

# Dependencies (not used in this no-build project, kept for safety)
node_modules/

# POLARIS（career-compass）は独立リポジトリとして別GitHubで管理するため除外
career-compass/

# AXONOM（DCフロアマップ）は独立リポジトリ（marutoke024-cloud/AXONOM）で管理するため除外
axonom/
